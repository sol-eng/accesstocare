# A list of examples in a folder

It uses the information from the 'metadata.yml' file to create a list
containing the name, description, type and path of each example content.

## Usage

``` r
folder_content_metadata(content_location = ".")
```

## Arguments

- content_location:

  The root folder of where the examples are located
