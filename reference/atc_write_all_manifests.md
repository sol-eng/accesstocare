# Batch creation of manifests

Attempts to create manifests for all sub-folders inside a given content
folder.

## Usage

``` r
atc_write_all_manifests(content_folder = ".")
```

## Arguments

- content_folder:

  The root folder location.
